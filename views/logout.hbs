{{!< layout }} <div class="flex justify-center items-center min-h-screen bg-gray-100">
  <div class="bg-white p-8 shadow-lg rounded-lg text-center">
    <h1 class="text-3xl font-bold mb-6">Logout</h1>
    <form method="GET" action="/auth/outlook/logout">
      <button type="submit"
        class="w-full bg-red-600 text-white text-lg font-semibold py-2 px-4 rounded-full shadow-md hover:bg-red-700 transition duration-300">
        Logout
      </button>
    </form>
  </div>
  </div>
